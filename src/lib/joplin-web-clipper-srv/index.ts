export * from './server';

/*
import { createJoplinWebClipperServer } from './server';

(async function () {
  const server = await createJoplinWebClipperServer();
  await server.start();
  console.info(`listening on ${server.info.uri}`);
})();
*/
